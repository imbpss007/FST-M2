apiVersion: v1
kind: Pod
metadata:
    name: client-pod
    labels:
        component: web
spec:
    containers:
        - name: client
          image: docker.io/imbpss007/complex_client
          ports:
              - containerPort: 3000